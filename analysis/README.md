To generate data, see data/README.
To run neural net, use:
```bash
python3 train.py ./data/
```
```THEANO_FLAGS=device=cpu python train.py data``` (for cpu)

The 'test/' directory probably isn't useful/doesn't work yet.

